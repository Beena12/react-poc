// Dummy Reducer for Header to get started with

const initialState = {

};

export const headerReducer = ( state = initialState, action ) => {
    switch (action.type) {
        case "":
            
            break;
    
        default:
            return state;
    }
}